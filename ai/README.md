# AI / NLP workspace (Student 4)

Pipeline: preprocess → TF-IDF → Multinomial Naive Bayes → negation/context → information extraction.

Missing impact fields stay `null`. The classifier must not fabricate deaths, injuries, displacement, missing persons, or damage.
