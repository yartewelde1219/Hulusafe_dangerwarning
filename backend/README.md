# Backend (Student 3)

FastAPI application with SQLAlchemy models for sources, news, danger events, regions, user locations, and notification deduplication.

Local default database is SQLite. Production/team integration uses PostgreSQL via `DATABASE_URL`.
